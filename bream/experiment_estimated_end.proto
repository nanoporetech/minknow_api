syntax="proto3";

package ont.bream.experiment_estimated_end;

// KEYSTORE KEY: bream.RUN_ESTIMATED_END_TIME

// This should be updated when a run begins and provide an estimate as to when
// the run will finish
//
// Should be specified in epoch time in seconds
message Experiment {
    uint64 estimated_end_time = 1;
}
